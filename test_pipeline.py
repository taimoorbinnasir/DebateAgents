# test_team_brainstorm.py — extend the existing test
from Week5.DebateAgents import run_team_round_loop

run_team_round_loop(
    topic="tea tastes better than coffee",
    max_rounds=2,
    session_id="test_team_002"
)